# Fix Render Deployment from Root

- [x] Update root `package.json` to add `start` script pointing to backend
- [x] Create root `render.yaml` for Render Blueprint auto-detection
- [x] Create root `Procfile` as fallback start command
- [x] Update `RENDER_DEPLOYMENT_GUIDE.md` with new deployment instructions

