{
  "name": "umuganda-t-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
"dev": "cd backend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd umugandapro && npm run dev",
    "install:all": "npm install && cd backend && npm install && cd ../umugandapro && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}

