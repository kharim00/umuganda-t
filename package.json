{
  "name": "umuganda-t-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd backend && npm start",
    "dev": "npm --prefix backend run dev",
    "install:all": "npm install && npm --prefix backend install && npm --prefix umugandapro install"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}