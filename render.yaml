services:
  - type: web
    name: umuganda-t
    env: node
    plan: free
    buildCommand: npm install && cd backend && npm install
    startCommand: cd backend && node index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000

